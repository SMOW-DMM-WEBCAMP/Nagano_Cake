<% provide(:title, "ログイン") %>
<div class="container">
  <div class="row">
    <div class="col-xs-8">
      <h3>会員の方はこちらからログイン</h3>
        <%= form_with model: @member, url: member_session_path, local: true do |f| %>
      <%= render "members/shared/error_messages", resource: resource %>
      <%= render "session_form_mail_pass", f: f %>
      <div class="col-xs-6">パスワードを忘れた方は
        <%= link_to "こちら", new_password_path(resource_name), class: "btn-link" %>
      </div>
      <div class="actions col-xs-6">
        <%= f.submit "ログイン", class: "btn btn-info" %>
      </div>
    <% end %>

      <div class="col-xs-9">
        <h4 class="head-line">会員登録がお済みではない方</h4>
        <p class="new-link">
          <%= link_to "こちら", new_registration_path(resource_name), data: {"turbolinks" => false}, class: "btn-link" %>　から新規登録を行ってください。
        </p>
      </div>
    </div>
  </div>
</div>